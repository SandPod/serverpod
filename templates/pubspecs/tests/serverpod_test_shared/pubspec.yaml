# TEMPLATE

publish_to: none

name: serverpod_test_shared
description: Code shared between the test server and client
version: 1.0.0

environment:
  sdk: DART_VERSION

dependencies:
  freezed_annotation: ^2.2.0
  json_annotation: ^4.8.0
  serverpod_serialization: SERVERPOD_VERSION

dev_dependencies:
  build_runner: ^2.4.1
  freezed: ^2.3.2
  json_serializable: ^6.6.1
  test: ^1.25.5
  serverpod_lints: SERVERPOD_VERSION

dependency_overrides:
  serverpod_serialization:
    path: ../../packages/serverpod_serialization
  serverpod_lints:
    path: ../../packages/serverpod_lints
